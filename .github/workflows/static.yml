<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Data Deletion Instructions</title>
  <meta name="description" content="Instructions for requesting deletion of user data associated with this Meta app." />
  <style>
    :root{
      --bg:#0b0e12; --card:#111827; --text:#e5e7eb; --muted:#9ca3af;
      --link:#60a5fa; --border:#1f2937; --accent:#22c55e;
    }
    body{
      margin:0; font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      background: var(--bg); color: var(--text); line-height:1.6;
    }
    header{ padding: 28px 16px 8px; max-width: 920px; margin: 0 auto; }
    main{ padding: 12px 16px 40px; max-width: 920px; margin: 0 auto; }
    .card{
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 18px;
      margin: 14px 0;
      box-shadow: 0 6px 24px rgba(0,0,0,.25);
    }
    h1{ font-size: 28px; margin: 0 0 8px; }
    h2{ font-size: 18px; margin: 0 0 10px; }
    p{ margin: 10px 0; color: var(--text); }
    .muted{ color: var(--muted); }
    a{ color: var(--link); text-decoration: none; }
    a:hover{ text-decoration: underline; }
    ul{ margin: 10px 0 0 18px; }
    .btn{
      display:inline-block; padding: 10px 14px; border-radius: 10px;
      background: var(--accent); color:#052e12; font-weight: 700;
      border: 0; margin-top: 10px;
    }
    .btn:hover{ filter: brightness(1.05); text-decoration:none; }
    footer{
      max-width: 920px; margin: 0 auto; padding: 10px 16px 30px;
      color: var(--muted); font-size: 13px;
    }
    code{
      background: rgba(255,255,255,.06);
      padding: 2px 6px; border-radius: 8px; border: 1px solid rgba(255,255,255,.08);
    }
  </style>
</head>

<body>
  <header>
    <h1>Data Deletion Instructions</h1>
    <p class="muted">
      This page explains how to request deletion of personal data associated with our Meta app.
    </p>
  </header>

  <main>
    <section class="card">
      <h2>1) How to Request Data Deletion</h2>
      <p>
        To request deletion of your personal data, please submit a request using the link below:
      </p>

      <!-- ✅ Replace the # with your real public form URL (Google Form / website form) -->
      <p>
        👉 <strong>Data Deletion Request Form:</strong><br/>
        <a href="#" id="formLink" rel="noopener">[INSERT YOUR FORM LINK HERE]</a>
      </p>

      <a class="btn" href="#" id="formBtn" rel="noopener">Open the Request Form</a>

      <p class="muted">
        Tip: Meta App Review typically expects a public URL and a clear process description.
      </p>
    </section>

    <section class="card">
      <h2>2) Information to Include</h2>
      <p>Please provide the following details so we can identify your data and process the request:</p>
      <ul>
        <li>Your full name</li>
        <li>Email address associated with your Facebook or Instagram account</li>
        <li>The platform used (Facebook or Instagram)</li>
        <li>A brief description of the data you want deleted</li>
      </ul>
    </section>

    <section class="card">
      <h2>3) What Happens Next</h2>
      <p>After we receive your request:</p>
      <ul>
        <li>We may verify your identity (if necessary)</li>
        <li>We will delete your personal data from our systems</li>
        <li>We will send you a confirmation once deletion is completed</li>
      </ul>
      <p>
        <strong>Processing time:</strong> up to <strong>30 days</strong>, in accordance with applicable data protection laws (GDPR) and Meta Platform requirements.
      </p>
    </section>

    <section class="card">
      <h2>4) Contact</h2>
      <p>
        If you have questions about this process, contact us:
      </p>

      <!-- ✅ Replace with your real contact email -->
      <p>
        📧 <strong>Email:</strong> <a href="mailto:your-email@example.com" id="contactEmail">your-email@example.com</a>
      </p>
    </section>

    <section class="card">
      <h2>5) Meta Platform Compliance</h2>
      <p>
        This procedure follows Meta’s official data deletion guidelines. You can learn more here:
      </p>
      <p>
        <a href="https://developers.facebook.com/docs/development/create-an-app/app-dashboard/data-deletion/" target="_blank" rel="noopener">
          Meta Documentation: Data Deletion Instructions
        </a>
      </p>
      <p class="muted">
        Note: This page is intended to be used as the <code>Data Deletion Instructions URL</code> in your Meta App settings.
      </p>
    </section>
  </main>

  <footer>
    © <span id="year"></span> — Data Deletion Instructions
  </footer>

  <script>
    // ✅ OPTIONAL: set your URLs here so you only edit once.
    const DATA_DELETION_FORM_URL = "https://myth.rip/sobaki_shukaki"; // <-- paste your public form URL
    const CONTACT_EMAIL = "ohlqndqz@aurevoirmail.com"; // <-- replace

    // Apply
    const formLink = document.getElementById("formLink");
    const formBtn = document.getElementById("formBtn");
    formLink.href = DATA_DELETION_FORM_URL;
    formBtn.href = DATA_DELETION_FORM_URL;

    const contactEmail = document.getElementById("contactEmail");
    contactEmail.href = "mailto:" + CONTACT_EMAIL;
    contactEmail.textContent = CONTACT_EMAIL;

    document.getElementById("year").textContent = new Date().getFullYear();
  </script>
</body>
</html>
